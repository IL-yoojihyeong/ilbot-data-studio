# docker compose용 환경 변수 — .env로 복사 후 수정
# 플랫폼 접속 비밀번호 (비우면 인증 없음 — 사내망 전용일 때만 권장)
ROBOLABEL_PASSWORD=change-me
ROBOLABEL_USER=admin
# 외부 포트 (기본 8322)
STUDIO_PORT=8322
# 데이터 저장 위치 (DB·원본·변환본·export 전부) — 절대경로 권장
STUDIO_DATA_DIR=./data
# 빌드되는 이미지의 태그 (보통 그대로 두면 됨)
STUDIO_TAG=latest
